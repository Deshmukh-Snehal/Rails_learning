<%= form_with(model: appoinmentinfo, local: true) do |form| %>
  <% if appoinmentinfo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(appoinmentinfo.errors.count, "error") %> prohibited this appoinmentinfo from being saved:</h2>

      <ul>
      <% appoinmentinfo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :appoinmentinfo %>
    <%= form.date_select :appoinmentinfo %>
  </div>

 <div class="field">
    <%= form.label "Doctor Name" %>
    <%= form.collection_select(:doctor_id, Doctor.all, :id, :name, { :prompt => 'Select a Doctor', :selected => @appoinmentinfo.doctor_id }, { class: 'form-control' }) %>
  </div>

  <div class="field">
    <%= form.label "Player Name" %>
    <%= form.collection_select(:player_id, Player.all, :id, :name, { :prompt => 'Select a Sport', :selected => @appoinmentinfo.player_id }, { class: 'form-control' }) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
