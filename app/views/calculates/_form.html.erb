<%= form_with(model: calculate, local: true) do |form| %>
  <% if calculate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(calculate.errors.count, "error") %> prohibited this calculate from being saved:</h2>

      <ul>
      <% calculate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
